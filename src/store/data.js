// state
export const state = () => ({

});

// mutations
export const mutations = {
	// set (state, { name, key, value }) {
	// 	state.object[name][key] = value;
	// },
	// countUp (state, { name, key }) {
	// 	state.object[name][key]++;
	// },
	// delete (state, name) {
	// 	delete state.object[name];
	// },
};
